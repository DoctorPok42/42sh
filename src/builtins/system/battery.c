/*
** EPITECH PROJECT, 2023
** 42sh
** File description:
** battery
*/

#include <stdio.h>
#include "mysh.h"

int func_battery(mysh_t *mysh, env_t *env, parser_t *parser)
{
    (void) mysh;
    (void) env;
    (void) parser;

    return 0;
}

    // system("upower -i /org/freedesktop/UPower/devices/battery_BAT0 | \
        //grep -E 'state|to\ full|to
        // empty|percentage|model|technology|battery|updated'");
